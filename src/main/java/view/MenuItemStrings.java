package view;

public class MenuItemStrings {
    public static final String[] menuItemStringsWithNoLoggedUser = {
            "EXIT from this program = EXIT",
            "Log in = LOGIN_USER",
            "Register = REGISTER_USER",
    };

    public static final String[] menuItemStringsWithLoggedUser = {
            "EXIT from this program = EXIT",
            "LOGOUT = LOGOUT_USER",
            "Add user = ADD_USER",
            "Edit user = EDIT_USER",
            "View user = VIEW_USER",
            "List all users = LIST_ALL_USERS",
            "Add quiz = ADD_QUIZ",
            "Edit quiz = EDIT_QUIZ",
            "View quiz = VIEW_QUIZ",
            "List all quizzes = LIST_ALL_QUIZZES",
            "List all quiz results = LIST_ALL_QUIZ_RESULTS",
            "Play Quiz = PLAY_QUIZ"
    };
}
